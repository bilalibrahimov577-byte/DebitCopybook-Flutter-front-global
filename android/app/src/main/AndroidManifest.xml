<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- İnternetə çıxış üçün icazə (reklamların yüklənməsi üçün vacibdir) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Borc Dəftəri"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Google AdMob üçün Tətbiq ID-si -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1488367137709334~6405570057"/>

        <!-- Flutter tərəfindən istifadə olunur -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Tətbiqin digər proqramları axtarması üçün icazələr bloku -->
    <queries>
        <!-- Bu, Flutter-in mətn emalı üçün istifadə etdiyi standart blokdur -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- === BAŞLA: BİZİM ƏLAVƏ ETDİYİMİZ HİSSƏ === -->
        <!-- Bu, email proqramlarını (mailto) aça bilmək üçün lazımdır -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <!-- === SON: BİZİM ƏLAVƏ ETDİYİMİZ HİSSƏ === -->
    </queries>

</manifest>